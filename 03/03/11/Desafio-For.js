const numeros = [100, 200, 300, 400, 500, 600];

// Primeira expressao eh executada uma unica vez
// Segunda expressao eh uma condicao de execucao 
// Terceira expressao eh excutada sempre ao final do bloco

for(let indice = 0; indice < numeros.length; indice++){
    console.log(numeros[indice]);
}